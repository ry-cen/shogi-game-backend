{
    "name": "shogi-game-server",
    "version": "1.0.0",
    "main": "app.js",
    "scripts" : {
        "start" : "node app.js",
        "build": "cd react-ui/ && npm install && npm run build"
    },
    "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.2"
  }
}
